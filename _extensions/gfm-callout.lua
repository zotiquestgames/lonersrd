-- RECONSTRUCTED (best-effort, no-op): the original of this file was never
-- opened during the session that built it, and was lost to an accidental
-- `git clean`. This is a safe passthrough so the build doesn't break;
-- Quarto callouts will render via Pandoc's default handling in GFM export
-- (no special GFM-flavored callout styling) until the real filter is
-- restored or rewritten.
return {}
